// function announceEvent(eventName, eventDate, eventDescription) {
//     const channel = member.channels.cache.find(ch => ch.name === 'announcements');
//     if (!channel) return;

//     const eventMessage = `
//     📅 **Event Announcement: ${eventName}**
    
//     🗓 Date: ${eventDate}
//     📖 Description: ${eventDescription}
    
//     Join us for an exciting event! Don't miss out on the fun and opportunities.
//     `;
    
//     channel.send(eventMessage);
// }

// // Example usage
// announceEvent('Game Night', 'July 20th, 8 PM', 'Join us for a night of fun games and prizes!');
